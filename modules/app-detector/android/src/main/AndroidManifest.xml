<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
    tools:ignore="ProtectedPermissions" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- Holding SYSTEM_ALERT_WINDOW grants the background-activity-launch exemption
       the service needs to bring up the full-screen ReminderActivity over the
       watched app. Unlike an accessibility service it does not trip Play
       Protect's install block. -->
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

  <!-- We only enumerate launchable apps (ACTION_MAIN/LAUNCHER) for the trigger-app
       picker and read their labels. That's all the package visibility we need, so
       we scope it with <queries> instead of the QUERY_ALL_PACKAGES permission,
       which Play flags as sensitive and requires a special declaration for. -->
  <queries>
    <intent>
      <action android:name="android.intent.action.MAIN" />
      <category android:name="android.intent.category.LAUNCHER" />
    </intent>
  </queries>

  <application>
    <service
      android:name="expo.modules.appdetector.AppMonitorService"
      android:enabled="true"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Responsible-gambling app-usage reminder" />
    </service>

    <!-- The full-screen "friction" reminder. Launched over a watched app instead
         of overlaying it. Its own task (empty taskAffinity), kept out of Recents,
         portrait-locked, and translucent so the dark scrim reads correctly. -->
    <activity
      android:name="expo.modules.appdetector.ReminderActivity"
      android:enabled="true"
      android:exported="false"
      android:launchMode="singleTask"
      android:taskAffinity=""
      android:excludeFromRecents="true"
      android:noHistory="true"
      android:screenOrientation="portrait"
      android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
      android:theme="@android:style/Theme.Material.NoActionBar" />
  </application>
</manifest>
