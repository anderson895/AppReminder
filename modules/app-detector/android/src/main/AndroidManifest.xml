<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
    tools:ignore="ProtectedPermissions" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- Draws the reminder pop-up card on top of a watched app. A plain overlay
       window (not an Activity), so it needs no Background-Activity-Launch
       exemption and — unlike an accessibility service — does not trip Play
       Protect's install block. -->
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

  <!-- We only enumerate launchable apps (ACTION_MAIN/LAUNCHER) for the trigger-app
       picker and read their labels. That's all the package visibility we need, so
       we scope it with <queries> instead of the QUERY_ALL_PACKAGES permission,
       which Play flags as sensitive and requires a special declaration for. -->
  <queries>
    <intent>
      <action android:name="android.intent.action.MAIN" />
      <category android:name="android.intent.category.LAUNCHER" />
    </intent>
  </queries>

  <application>
    <service
      android:name="expo.modules.appdetector.AppMonitorService"
      android:enabled="true"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Responsible-gambling app-usage reminder" />
    </service>
  </application>
</manifest>
