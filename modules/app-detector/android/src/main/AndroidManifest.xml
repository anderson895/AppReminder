<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
    tools:ignore="ProtectedPermissions" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
  <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
    tools:ignore="QueryAllPackagesPermission" />

  <application>
    <service
      android:name="expo.modules.appdetector.AppMonitorService"
      android:enabled="true"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Responsible-gambling app-usage reminder" />
    </service>

    <!-- Full-screen reminder shown over a trigger app. A real activity (not an
         overlay) so security-hardened apps like GCash can't hide it. Locked to
         portrait; excluded from recents so it can't be swiped back to. -->
    <activity
      android:name="expo.modules.appdetector.BlockerActivity"
      android:enabled="true"
      android:exported="false"
      android:screenOrientation="portrait"
      android:excludeFromRecents="true"
      android:launchMode="singleTask"
      android:taskAffinity="expo.modules.appdetector.blocker"
      android:noHistory="true"
      android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
      android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen" />
  </application>
</manifest>
