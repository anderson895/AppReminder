# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
*.expo-export*/

# native (root prebuild output only — NOT modules/*/android, which is real source)
/ios/
/android/

# generated build outputs inside local modules (NOT the module source itself)
modules/*/android/build/
modules/*/android/.gradle/

# misc
.DS_Store
*.log
npm-debug.*

# typescript
expo-env.d.ts
project_details.txt
.claude
# release artifacts (uploaded to GitHub Releases, not committed)
/release/

# local env (Firebase credentials live here, never committed)
.env
.env.*
!.env.example
